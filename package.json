{
  "name": "autocomplete-kit-root",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for @gopala/autocomplete-kit library and demo app",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "echo \"(will add ESLint later)\"",
    "format": "echo \"(will add Prettier later)\"",
    "typecheck": "echo \"(will add TypeScript project refs later)\"",
    "test": "echo \"(will add Vitest later)\"",
    "build": "pnpm -r build",
    "ci": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "license": "MIT",
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.13"
  }
}
